# Define a data.frame, `ingredients`,
# with 3 possible sets of ingredients for a dough recipe.
ingredients <- data.frame(
  grains = c(100, 200, 300),
  yeast = c(1, 2, 3),
  water = c(200, 300, 400),
  salt = c(5, 10, 15)
)
